using UnityEngine;
using UnityEditor;
using UnityEditor.Callbacks;

public class BuildPacketPostProcess : ScriptableObject {

    //[PostProcessBuild]
    //public static void OnPostprocessBuild(BuildTarget buildTarget, string path) {
    //    if (buildTarget == BuildTarget.iOS) {
    //        BuildPacketPostProcess_iOS.PostProc(path);
    //    }
    //}

}